@model HotelDiamond.Models.User
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Авторизация";
    string pattern = @"^(?("")(""[^""]+?""@)|(([0-9a-z]((\.(?!\.))|[-!#\$%&'\*\+/=\?\^`\{\}\|~\w])*)(?<=[0-9a-z])@))" +
                @"(?(\[)(\[(\d{1,3}\.){3}\d{1,3}\])|(([0-9a-z][-\w]*[0-9a-z]*\.)+[a-z0-9]{2,17}))$";

}
@using System.Text.RegularExpressions;

<form method="post" action="">
    <input type="hidden" value="@ViewBag.BookId" name="BookId" />
    <table>
        <tr>
            <td><p>Введите свое имя </p></td>
            <td><input type="text" name="Person" /> </td>
        </tr>
        <tr>
            <td><p>Введите адрес :</p></td>
            <td>
                <input type="text" name="Address" />
            </td>
        </tr>
        <tr>
            <td><input type="submit" value="Отправить" /> </td>
            <td></td>
        </tr>
    </table>
</form>
@" "@
@using (Html.BeginForm("Autorization", "Home", FormMethod.Post))
{
    @Html.HiddenFor(m => m.ID)
    @Html.LabelFor(m => m.UserName, "Логин")
    <br />
    @Html.TextBoxFor(m => m.UserName)
    <br />
    @Html.LabelFor(m => m.Fio, "ФИО")
    <br />
    @Html.TextBoxFor(m => m.Fio)
    <br />
    @Html.LabelFor(m => m.Email, "Email")
    <br />
    @Html.TextBoxFor(m => m.Email)
    <br />
    @Html.LabelFor(m => m.Password, "Пароль")
    <br />
    @Html.PasswordFor(m => m.Password)
    <input type="submit" value="Отправить" />
}
    @*
        @if (Regex.IsMatch(m.Email, pattern, RegexOptions.IgnoreCase) == true)
        {

            <input name="Enable" type="hidden" value="false" />
            <input type="submit" value="Отправить" />
        }
        else
        {
            <input id="Enable" name="Enable" type="submit" value="false" />
        }
    *@




